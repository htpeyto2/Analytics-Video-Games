Open Power BI Desktop, then go to Get Data → Text/CSV and pick OSR/data/prayer_cost_vs_time.csv.
Add a scatter chart with hours_to_99 on the X axis, cost_to_99 on the Y axis, name in Values (so each bone is its own dot), and on_frontier in Legend.
Set the X axis to a log scale so Bones doesn't squash everything else.
Save the .pbix into the OSR folder and export a screenshot for the README.